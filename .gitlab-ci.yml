stages:
  - cluster-setup
  - infra-setup
  - stateful-apps-deploy
  - apps-loadgen
  - litmus-chaos
  - cleanup
  - deploy

azure-cluster:
  image: atulabhi/kops:v8
  stage: cluster-setup
  script: 
    - chmod 755 ./script/azure
    - ./script/azure

  artifacts:
    paths:
      - azoebs_aks_name/

openebs-deploy:
  image: atulabhi/kops:v8
  stage: infra-setup
  dependencies:
    - azure-cluster
  script: 
   - echo "deploying OpenEBS on Azure"
   - ls
   - chmod 775 ./script/infra-setup
   - ./script/infra-setup
  
  artifacts:
    paths:
      - azure/

percona-jiva:
  image: atulabhi/kops:v8
  stage: stateful-apps-deploy
  dependencies:
    - openebs-deploy
  script: 
   - echo "jobs-Azure-Started"
   - chmod 775 ./script/percona-app-deploy
   - ./script/percona-app-deploy

  artifacts:
    paths:
      - azure/

jenkins-jiva:
  image: atulabhi/kops:v8
  stage: stateful-apps-deploy
  dependencies:
    - openebs-deploy
  script: 
   - echo "jobs-Azure-Started"
   - chmod 775 ./script/jenkins-app-deploy
   - ./script/jenkins-app-deploy

  artifacts:
    paths:
      - azure/

cassandra-jiva:
  image: atulabhi/kops:v8
  stage: stateful-apps-deploy
  dependencies:
    - openebs-deploy
  script: 
   - echo "jobs-Azure-Started"
   - chmod 775 ./script/cassandra-app-deploy
   - ./script/cassandra-app-deploy

  artifacts:
    paths:
      - azure/

mongodb-jiva:
  image: atulabhi/kops:v8
  stage: stateful-apps-deploy
  dependencies:
    - openebs-deploy
  script: 
   - echo "jobs-Azure-Started"
   - chmod 775 ./script/mongodb-app-deploy
   - ./script/mongodb-app-deploy

  artifacts:
    paths:
      - azure/

postgres-jiva:
  image: atulabhi/kops:v8
  stage: stateful-apps-deploy
  dependencies:
    - openebs-deploy
  script: 
   - echo "jobs-Azure-Started"
   - chmod 775 ./script/postgres-app-deploy
   - ./script/postgres-app-deploy

  artifacts:
    paths:
      - azure/

cassandra-loadgen:
  image: atulabhi/kops:v8
  stage: apps-loadgen
  dependencies:
    - cassandra-jiva
  script: 
   - echo "jobs-Azure-Started"
   - chmod 775 ./script/cassandra-loadgen
   - ./script/cassandra-loadgen

  artifacts:
    paths:
      - azure/

percona-loadgen:
  image: atulabhi/kops:v8
  stage: apps-loadgen
  dependencies:
    - percona-jiva
  script: 
   - echo "jobs-Azure-Started"
   - chmod 775 ./script/percona-loadgen
   - ./script/percona-loadgen

  artifacts:
    paths:
      - azure/

mongodb-loadgen:
  image: atulabhi/kops:v8
  stage: apps-loadgen
  dependencies:
    - mongodb-jiva
  script: 
   - echo "jobs-Azure-Started"
   - chmod 775 ./script/mongodb-loadgen
   - ./script/mongodb-loadgen

  artifacts:
    paths:
      - azure/

litmus-chaos:
  image: atulabhi/kops:v8
  dependencies:
    - percona-loadgen
  stage: litmus-chaos
  script:
   - chmod 775 ./script/app-chaos
   - ./script/app-chaos

cleanup-azure:
  when: always
  image: atulabhi/kops:v8
  dependencies:
    - azure-cluster
  stage: cleanup
  script: 
    - chmod 755 ./script/azure-cleanup
    - ./script/azure-cleanup
